# Dependencies
node_modules/

# Next.js
.next/
out/

# Env and secrets (never commit)
.env
.env.local
.env.production
.env.vercel
.env.backup
.env*.local
.env.*.local
# Broad guard — keep the committed template allowed (see below)
.env*
!.env.example

# Local archive of old env files (compromised / retired values)
.env-archive/

# Debug
npm-debug.log*
.pnp.*

# Vercel CLI link metadata (relink per machine / project as needed)
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
*.swp
.DS_Store
