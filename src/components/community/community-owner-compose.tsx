"use client";

/**
 * @deprecated Creation moved to contextual page headers (Spaces / Space pages).
 * Kept as a no-op export so stale imports fail visibly during refactors.
 */
export function CommunityOwnerCompose() {
  return null;
}
