/** CommunityBottomSheet overlay/content (see community-bottom-sheet.tsx). */
export const MH_LAYER_SHEET_OVERLAY = 60;
export const MH_LAYER_SHEET_CONTENT = 61;

/** Menus/popovers portaled above sheets and default dialogs (z-50). */
export const MH_LAYER_MENU = 100;

/** Confirm dialogs opened from inside a bottom sheet. */
export const MH_LAYER_SHEET_DIALOG = 110;

/** Toasts while a sheet is open. */
export const MH_LAYER_TOAST = 115;
